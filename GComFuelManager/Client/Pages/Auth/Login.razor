@layout FullPageComponent
@using GComFuelManager.Client.Auth;
@page "/login"
@inject NavigationManager navigationManager
@inject SweetAlertService swal


<div class="col-12 card shadow">


    <section class="vh-100" style="background-color: #ffffff;">
        <div class="container py-5 h-100">
            <div class="row d-flex justify-content-center align-items-center h-100">
                <div class="col col-xl-10">
                    <div class="card shadow-lg" style="border-radius: 1rem; ">
                        <div class="row g-0">
                            <div class="col-md-6 d-flex justify-content-center align-items-center gcom-bg-rojo" style="border-radius:4px;">
                                <div class="col-6">
                                    <img src="img/gcom_unilogo.png"
                                         alt="login form" class=" img-fluid" />
                                </div>
                            </div>
                            <div class="col-6 d-flex align-items-center">
                                <div class="card-body p-4 p-lg-5 text-black">

                                    <EditForm Model="usuario">
                                        <DataAnnotationsValidator />
                                        <div class="d-flex align-items-center mb-3 pb-1">
                                            <i class="fas fa-cubes fa-2x me-3" style="color: #ff6219;"></i>
                                        </div>
                                        <div class="form-outline mb-4">
                                            <label class="form-label" for="form2Example17">Ingrese su Usuario:</label>
                                            <InputText class="form-control" placeholder="Nombre de usuario" @bind-Value="usu"/>
                                            <ValidationMessage For="@(()=>usu)" />
                                        </div>

                                        <div class="form-outline mb-2">
                                            <label class="form-label" for="form2Example27">Ingrese su contraseña:</label>
                                            <InputText type="password" class="form-control" placeholder="@placeholderPswd" @bind-Value="cve"/>
                                            <ValidationMessage For="@(()=>cve)" />
                                        </div>
                                        <div class="col-12 mt-4 d-flex justify-content-center" >
                                            <button class="btn gcom-bg-amarillo btn-lg px-5" type="button">Ingresar</button>
                                        </div>

                                    </EditForm>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

</div>


@code {
    private Usuario usuario { get; set; } = new Usuario();
    private string usu = string.Empty;
    private string cve = string.Empty;
    string placeholderPswd = "**********";
    private async Task LoguearUsuario()
    {
        //Mandamos al usuario, lo construimos y retornamos con su token

    }
}

<!--Usu, cve-->
<!--
    <div class="form-group">
            <label>Ingrese su Usuario:</label>
            <div>
                <InputText class="form-control" -Value="usu" />
                <ValidationMessage For="" />
            </div>
        </div>

        <div class="form-group">
            <label>Ingrese su contraseña:</label>
            <div>
                <InputText type="password" class="form-control" -Value="cve" />
                <ValidationMessage For="" />
            </div>
        </div>
        <button class="btn gcom-bg-amarillo col-12">Ingresar</button>


-->
